# ChatGPT-native candidate surface for later Agda certification.
# External Haskell/Elixir/Clojure traces only falsify/confirm finite instances.
# Discovery policy: equality-saturation / graph search first; bounded proof search
# second; MCTS only for branching proof-tree exploration; Random Search/SimpleES
# are useful for counterexample generation and numeric stress cases, not identity proof.
compose(affine,layerNorm)
compose(compose(affine,layerNorm),outputTanh)
reverse(compose(f,g))
iterate(recurrentStep,k)
normalize(layerNorm,x)
reverse(compose(f,g)) = compose(reverse(g),reverse(f))
lstmRun(append(xs,ys),s) = lstmRun(ys,lstmRun(xs,s))
cell' = forget * oldCell + input * candidate
hidden' = output * tanh(cell')
sharedNode.primal-and-pullback = one-definitional-program
