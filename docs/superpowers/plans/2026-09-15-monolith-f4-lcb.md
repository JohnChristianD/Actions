# Full coupled monolith consolidation

The authoritative learner theorem is the Agda monolith. The optimizer is F4-Int-U(p), with its sigma-delta residual state, coupled global L2, norm-pair, q-budget, and exact reconstruction invariants. Sparsemax uses deterministic count-conditioned LCB score modifiers expressed through an exact dyadic rational interface and an Int8 policy-facing encoding. Statistical-confidence claims and environment-specific learner examples are outside the canonical theorem surface.

The strongest promotion target is the deterministic compositional generalization theorem: equal-count LCB is a common score shift at the exact score layer, so an independently proven sparsemax common-shift invariance transfers ceteris-paribus LCB/no-LCB equivalence through the composition. Thompson-sampling equivalence is not promoted because the canonical transition is deterministic and has no posterior sampling semantics.
