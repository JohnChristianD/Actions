name: Agda authoritative safe gate

on:
  pull_request:
    branches: ['main']
    paths:
      - '**/*.agda'
      - '.github/workflows/agda.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  kernel:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: agda/agda-setup-action@v1.0.0
        with:
          agda-version: '2.8.0'
      - name: Verify Agda executable
        run: agda --version
      - name: Safe-check v154 finite ordered Efficient-CHAD target
        run: agda --safe Exotic/ERL/FullCoupled/EfficientCHAD_v154.agda
      - name: Verify active target exclusions
        shell: bash
        run: |
          set -euo pipefail
          ! grep -nE 'LayerNorm|BatchNorm|BatchRenorm|Certificate|setup-python|ruby/setup-ruby|python3|StoSignSGD' Exotic/ERL/FullCoupled/EfficientCHAD_v154.agda
