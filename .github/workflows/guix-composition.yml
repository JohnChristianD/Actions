name: Guix connected composition verification

on:
  push:
    branches: [main, guix-composition-ci]
  pull_request:
    branches: [main]
  schedule:
    - cron: "17 * * * *"
  workflow_dispatch:
  workflow_call:

concurrency:
  group: guix-connected-composition-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  agda-safe:
    name: Guix / Agda --safe connected theorem surface
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    container:
      image: jas4711/guix@sha256:1ecca8f90bdc59d0fac97700aa18e6e0677de8b75dc9ffc16bf7452b240d8f52
      options: --security-opt seccomp=unconfined
    defaults:
      run:
        shell: sh
    steps:
      - name: Initialize Guix daemon
        run: |
          set -eu
          groupadd --system guixbuild 2>/dev/null || true
          build_shell="$(command -v nologin || command -v false || echo /bin/sh)"
          for i in $(seq -w 1 10); do
            useradd -g guixbuild -G guixbuild -d /var/empty -s "$build_shell" \
              -c "Guix build user $i" --system guixbuilder$i 2>/dev/null || true
          done
          mkdir -p /var/guix/daemon-socket
          if [ ! -S /var/guix/daemon-socket/socket ]; then
            env LANG=C.UTF-8 guix-daemon --disable-chroot \
              --build-users-group=guixbuild > /tmp/guix-daemon.log 2>&1 &
          fi
          for i in $(seq 1 30); do
            if [ -S /var/guix/daemon-socket/socket ]; then
              break
            fi
            sleep 1
          done
          test -S /var/guix/daemon-socket/socket
          if [ -f /share/guix/ci.guix.gnu.org.pub ]; then
            guix archive --authorize < /share/guix/ci.guix.gnu.org.pub
          fi
          if [ -f /share/guix/bordeaux.guix.gnu.org.pub ]; then
            guix archive --authorize < /share/guix/bordeaux.guix.gnu.org.pub
          fi

      - name: Initialize container network service database
        run: |
          set -eu
          touch /etc/services
          grep -q '^http[[:space:]]\+80/tcp' /etc/services || \
            printf '%s\n' 'http 80/tcp' >> /etc/services
          grep -q '^https[[:space:]]\+443/tcp' /etc/services || \
            printf '%s\n' 'https 443/tcp' >> /etc/services
          grep -q '^git[[:space:]]\+9418/tcp' /etc/services || \
            printf '%s\n' 'git 9418/tcp' >> /etc/services

      - name: Prepare pinned Guix channel with container Git
        run: |
          set -eu
          rm -rf /tmp/guix-channel
          git config --global --add safe.directory /tmp/guix-channel
          git clone --no-tags --single-branch --branch version-1.5.0 \
            "https://github.com/neuroradiology/guix-mirror.git" /tmp/guix-channel
          git -C /tmp/guix-channel fetch origin \
            "ac03c482b1910a1672427beaea07ddcd1d652806"
          git -C /tmp/guix-channel checkout --detach \
            "ac03c482b1910a1672427beaea07ddcd1d652806"
          test "$(git -C /tmp/guix-channel rev-parse HEAD)" = \
            "ac03c482b1910a1672427beaea07ddcd1d652806"
          printf '%s\n' \
            "(list" \
            " (channel" \
            "  (name 'guix)" \
            "  (url \"file:///tmp/guix-channel\")" \
            "  (commit \"ac03c482b1910a1672427beaea07ddcd1d652806\"))" \
            ")" > /tmp/channels-ci.scm

      - name: Checkout repository with container Git
        run: |
          set -eu
          test -x "$(command -v git)"
          git --version
          workspace="${GITHUB_WORKSPACE}"
          mkdir -p "${workspace}"
          git config --global --add safe.directory "${workspace}"
          git -C "${workspace}" init -q
          git -C "${workspace}" remote remove origin >/dev/null 2>&1 || true
          git -C "${workspace}" remote add origin \
            "https://github.com/${GITHUB_REPOSITORY}.git"
          git -C "${workspace}" fetch --depth=1 origin "${GITHUB_SHA}"
          git -C "${workspace}" checkout --detach "${GITHUB_SHA}"
      - name: Run pinned Guix Agda lane
        env:
          CI_LANE: agda-safe
        run: |
          guix time-machine -C /tmp/channels-ci.scm -- \
            shell --pure -m .guix/manifest.scm -- guile .guix/ci.scm

  mercury:
    name: Guix / Mercury connected theorem and verifier lane
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    container:
      image: jas4711/guix@sha256:1ecca8f90bdc59d0fac97700aa18e6e0677de8b75dc9ffc16bf7452b240d8f52
      options: --security-opt seccomp=unconfined
    defaults:
      run:
        shell: sh
    steps:
      - name: Initialize Guix daemon
        run: |
          set -eu
          groupadd --system guixbuild 2>/dev/null || true
          build_shell="$(command -v nologin || command -v false || echo /bin/sh)"
          for i in $(seq -w 1 10); do
            useradd -g guixbuild -G guixbuild -d /var/empty -s "$build_shell"               -c "Guix build user $i" --system guixbuilder$i 2>/dev/null || true
          done
          mkdir -p /var/guix/daemon-socket
          if [ ! -S /var/guix/daemon-socket/socket ]; then
            env LANG=C.UTF-8 guix-daemon --disable-chroot \
              --build-users-group=guixbuild > /tmp/guix-daemon.log 2>&1 &
          fi
          for i in $(seq 1 30); do
            if [ -S /var/guix/daemon-socket/socket ]; then
              break
            fi
            sleep 1
          done
          test -S /var/guix/daemon-socket/socket
          if [ -f /share/guix/ci.guix.gnu.org.pub ]; then
            guix archive --authorize < /share/guix/ci.guix.gnu.org.pub
          fi
          if [ -f /share/guix/bordeaux.guix.gnu.org.pub ]; then
            guix archive --authorize < /share/guix/bordeaux.guix.gnu.org.pub
          fi

      - name: Initialize container network service database
        run: |
          set -eu
          touch /etc/services
          grep -q '^http[[:space:]]\+80/tcp' /etc/services || \
            printf '%s\n' 'http 80/tcp' >> /etc/services
          grep -q '^https[[:space:]]\+443/tcp' /etc/services || \
            printf '%s\n' 'https 443/tcp' >> /etc/services
          grep -q '^git[[:space:]]\+9418/tcp' /etc/services || \
            printf '%s\n' 'git 9418/tcp' >> /etc/services

      - name: Prepare pinned Guix channel with container Git
        run: |
          set -eu
          rm -rf /tmp/guix-channel
          git config --global --add safe.directory /tmp/guix-channel
          git clone --no-tags --single-branch --branch version-1.5.0 \
            "https://github.com/neuroradiology/guix-mirror.git" /tmp/guix-channel
          git -C /tmp/guix-channel fetch origin \
            "ac03c482b1910a1672427beaea07ddcd1d652806"
          git -C /tmp/guix-channel checkout --detach \
            "ac03c482b1910a1672427beaea07ddcd1d652806"
          test "$(git -C /tmp/guix-channel rev-parse HEAD)" = \
            "ac03c482b1910a1672427beaea07ddcd1d652806"
          printf '%s\n' \
            "(list" \
            " (channel" \
            "  (name 'guix)" \
            "  (url \"file:///tmp/guix-channel\")" \
            "  (commit \"ac03c482b1910a1672427beaea07ddcd1d652806\"))" \
            ")" > /tmp/channels-ci.scm

      - name: Checkout repository with container Git
        run: |
          set -eu
          test -x "$(command -v git)"
          git --version
          workspace="${GITHUB_WORKSPACE}"
          mkdir -p "${workspace}"
          git config --global --add safe.directory "${workspace}"
          git -C "${workspace}" init -q
          git -C "${workspace}" remote remove origin >/dev/null 2>&1 || true
          git -C "${workspace}" remote add origin \
            "https://github.com/${GITHUB_REPOSITORY}.git"
          git -C "${workspace}" fetch --depth=1 origin "${GITHUB_SHA}"
          git -C "${workspace}" checkout --detach "${GITHUB_SHA}"
      - name: Run pinned Guix Mercury lane
        env:
          CI_LANE: mercury
        run: |
          guix time-machine -C /tmp/channels-ci.scm -- \
            shell --pure -m .guix/manifest.scm -- guile .guix/ci.scm

  discovery:
    name: Guix / Mercury semantic e-graph discovery
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    container:
      image: jas4711/guix@sha256:1ecca8f90bdc59d0fac97700aa18e6e0677de8b75dc9ffc16bf7452b240d8f52
      options: --security-opt seccomp=unconfined
    defaults:
      run:
        shell: sh
    steps:
      - name: Initialize Guix daemon
        run: |
          set -eu
          groupadd --system guixbuild 2>/dev/null || true
          build_shell="$(command -v nologin || command -v false || echo /bin/sh)"
          for i in $(seq -w 1 10); do
            useradd -g guixbuild -G guixbuild -d /var/empty -s "$build_shell"               -c "Guix build user $i" --system guixbuilder$i 2>/dev/null || true
          done
          mkdir -p /var/guix/daemon-socket
          if [ ! -S /var/guix/daemon-socket/socket ]; then
            env LANG=C.UTF-8 guix-daemon --disable-chroot \
              --build-users-group=guixbuild > /tmp/guix-daemon.log 2>&1 &
          fi
          for i in $(seq 1 30); do
            if [ -S /var/guix/daemon-socket/socket ]; then
              break
            fi
            sleep 1
          done
          test -S /var/guix/daemon-socket/socket
          if [ -f /share/guix/ci.guix.gnu.org.pub ]; then
            guix archive --authorize < /share/guix/ci.guix.gnu.org.pub
          fi
          if [ -f /share/guix/bordeaux.guix.gnu.org.pub ]; then
            guix archive --authorize < /share/guix/bordeaux.guix.gnu.org.pub
          fi

      - name: Initialize container network service database
        run: |
          set -eu
          touch /etc/services
          grep -q '^http[[:space:]]\+80/tcp' /etc/services || \
            printf '%s\n' 'http 80/tcp' >> /etc/services
          grep -q '^https[[:space:]]\+443/tcp' /etc/services || \
            printf '%s\n' 'https 443/tcp' >> /etc/services
          grep -q '^git[[:space:]]\+9418/tcp' /etc/services || \
            printf '%s\n' 'git 9418/tcp' >> /etc/services

      - name: Prepare pinned Guix channel with container Git
        run: |
          set -eu
          rm -rf /tmp/guix-channel
          git config --global --add safe.directory /tmp/guix-channel
          git clone --no-tags --single-branch --branch version-1.5.0 \
            "https://github.com/neuroradiology/guix-mirror.git" /tmp/guix-channel
          git -C /tmp/guix-channel fetch origin \
            "ac03c482b1910a1672427beaea07ddcd1d652806"
          git -C /tmp/guix-channel checkout --detach \
            "ac03c482b1910a1672427beaea07ddcd1d652806"
          test "$(git -C /tmp/guix-channel rev-parse HEAD)" = \
            "ac03c482b1910a1672427beaea07ddcd1d652806"
          printf '%s\n' \
            "(list" \
            " (channel" \
            "  (name 'guix)" \
            "  (url \"file:///tmp/guix-channel\")" \
            "  (commit \"ac03c482b1910a1672427beaea07ddcd1d652806\"))" \
            ")" > /tmp/channels-ci.scm

      - name: Checkout repository with container Git
        run: |
          set -eu
          test -x "$(command -v git)"
          git --version
          workspace="${GITHUB_WORKSPACE}"
          mkdir -p "${workspace}"
          git config --global --add safe.directory "${workspace}"
          git -C "${workspace}" init -q
          git -C "${workspace}" remote remove origin >/dev/null 2>&1 || true
          git -C "${workspace}" remote add origin \
            "https://github.com/${GITHUB_REPOSITORY}.git"
          git -C "${workspace}" fetch --depth=1 origin "${GITHUB_SHA}"
          git -C "${workspace}" checkout --detach "${GITHUB_SHA}"
      - name: Run pinned Mercury semantic e-graph discovery lane
        env:
          CI_LANE: discovery
        run: |
          guix time-machine -C /tmp/channels-ci.scm -- \
            shell --pure -m .guix/manifest.scm -- guile .guix/ci.scm
      - name: Verify forced endogenous theorem discovery artifact is present
        run: |
          set -eu
          test -f ".ci/discovery/novel-learner-theorem-discovery.json"
          printf '%s\n' "novel-learner-theorem-discovery-report=present"

  surface:
    name: Guix / canonical source-policy surface
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    container:
      image: jas4711/guix@sha256:1ecca8f90bdc59d0fac97700aa18e6e0677de8b75dc9ffc16bf7452b240d8f52
      options: --security-opt seccomp=unconfined
    defaults:
      run:
        shell: sh
    steps:
      - name: Initialize Guix daemon
        run: |
          set -eu
          groupadd --system guixbuild 2>/dev/null || true
          build_shell="$(command -v nologin || command -v false || echo /bin/sh)"
          for i in $(seq -w 1 10); do
            useradd -g guixbuild -G guixbuild -d /var/empty -s "$build_shell"               -c "Guix build user $i" --system guixbuilder$i 2>/dev/null || true
          done
          mkdir -p /var/guix/daemon-socket
          if [ ! -S /var/guix/daemon-socket/socket ]; then
            env LANG=C.UTF-8 guix-daemon --disable-chroot \
              --build-users-group=guixbuild > /tmp/guix-daemon.log 2>&1 &
          fi
          for i in $(seq 1 30); do
            if [ -S /var/guix/daemon-socket/socket ]; then
              break
            fi
            sleep 1
          done
          test -S /var/guix/daemon-socket/socket
          if [ -f /share/guix/ci.guix.gnu.org.pub ]; then
            guix archive --authorize < /share/guix/ci.guix.gnu.org.pub
          fi
          if [ -f /share/guix/bordeaux.guix.gnu.org.pub ]; then
            guix archive --authorize < /share/guix/bordeaux.guix.gnu.org.pub
          fi

      - name: Initialize container network service database
        run: |
          set -eu
          touch /etc/services
          grep -q '^http[[:space:]]\+80/tcp' /etc/services || \
            printf '%s\n' 'http 80/tcp' >> /etc/services
          grep -q '^https[[:space:]]\+443/tcp' /etc/services || \
            printf '%s\n' 'https 443/tcp' >> /etc/services
          grep -q '^git[[:space:]]\+9418/tcp' /etc/services || \
            printf '%s\n' 'git 9418/tcp' >> /etc/services

      - name: Prepare pinned Guix channel with container Git
        run: |
          set -eu
          rm -rf /tmp/guix-channel
          git config --global --add safe.directory /tmp/guix-channel
          git clone --no-tags --single-branch --branch version-1.5.0 \
            "https://github.com/neuroradiology/guix-mirror.git" /tmp/guix-channel
          git -C /tmp/guix-channel fetch origin \
            "ac03c482b1910a1672427beaea07ddcd1d652806"
          git -C /tmp/guix-channel checkout --detach \
            "ac03c482b1910a1672427beaea07ddcd1d652806"
          test "$(git -C /tmp/guix-channel rev-parse HEAD)" = \
            "ac03c482b1910a1672427beaea07ddcd1d652806"
          printf '%s\n' \
            "(list" \
            " (channel" \
            "  (name 'guix)" \
            "  (url \"file:///tmp/guix-channel\")" \
            "  (commit \"ac03c482b1910a1672427beaea07ddcd1d652806\"))" \
            ")" > /tmp/channels-ci.scm

      - name: Checkout repository with container Git
        run: |
          set -eu
          test -x "$(command -v git)"
          git --version
          workspace="${GITHUB_WORKSPACE}"
          mkdir -p "${workspace}"
          git config --global --add safe.directory "${workspace}"
          git -C "${workspace}" init -q
          git -C "${workspace}" remote remove origin >/dev/null 2>&1 || true
          git -C "${workspace}" remote add origin \
            "https://github.com/${GITHUB_REPOSITORY}.git"
          git -C "${workspace}" fetch --depth=1 origin "${GITHUB_SHA}"
          git -C "${workspace}" checkout --detach "${GITHUB_SHA}"
      - name: Audit repository surface
        env:
          CI_LANE: surface
        run: |
          guix time-machine -C /tmp/channels-ci.scm -- \
            shell --pure -m .guix/manifest.scm -- guile .guix/ci.scm
