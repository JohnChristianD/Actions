name: Functional oracle gate

on:
  push:
    branches: ["v147-q-chad-agda-safe"]
  pull_request:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  haskell-agda2hs:
    name: Haskell oracle via Agda2hs
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Setup Nix
        uses: DeterminateSystems/nix-installer-action@main
      - name: Generate Haskell with Agda2hs
        run: nix run github:agda/agda2hs -- -i. oracles/QClosurePredictive_v147.agda
      - name: Setup Haskell Cabal
        uses: haskell-actions/setup@v2
        with:
          cabal-version: 'latest'
      - name: Compile generated Haskell oracle through Cabal
        run: cabal exec -- ghc -fno-code oracles/QClosurePredictive_v147.hs

  elixir:
    name: Elixir functional oracle
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: erlef/setup-beam@v1
        with:
          elixir-version: '1.18'
          otp-version: '27'
      - name: Run oracle
        run: elixir oracles/q_closure_predictive_v147.exs

  clojure:
    name: Clojure functional oracle
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: DeLaGuardo/setup-clojure@13
        with:
          cli: '1.11.1.1435'
      - name: Run oracle
        run: clojure -M oracles/q_closure_predictive_v147.clj
