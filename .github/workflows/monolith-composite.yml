name: Monolith composite proof gate

on:
  push:
    branches: ['main']
    paths:
      - 'Exotic/ERL/FullCoupled/GeneralFullCoupledLearnerMonolith.agda'
      - 'Exotic/ERL/FullCoupled/GeneralFullCoupledTheoremsMonolith.agda'
      - 'Exotic/ERL/FullCoupled/MonolithCompositeReservoirTheorem.agda'
      - 'Exotic/ERL/FullCoupled/FiniteCyclicNormPairCertificate.agda'
      - 'Exotic/ERL/FullCoupled/SignedDivisibilityBridge.agda'
      - 'Exotic/ERL/FullCoupled/FiniteUniversalBoundary.agda'
      - '.ci/discovery/ProofSearch.py'
      - '.github/workflows/monolith-composite.yml'
  workflow_dispatch:

concurrency:
  group: monolith-composite-main
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  composite:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.sha || github.sha }}

      - name: Set up Agda and standard library
        uses: agda/agda-setup-action@v1
        with:
          agda-version: '2.8.0'
          agda-stdlib-version: '2.4'

      - name: Check learner monolith
        run: timeout 180s agda --safe Exotic/ERL/FullCoupled/GeneralFullCoupledLearnerMonolith.agda

      - name: Check coupled composite theorem
        run: timeout 120s agda --safe Exotic/ERL/FullCoupled/MonolithCompositeReservoirTheorem.agda

      - name: Check finite cyclic norm-pair certificate
        run: timeout 120s agda --safe Exotic/ERL/FullCoupled/FiniteCyclicNormPairCertificate.agda

      - name: Check finite universal boundary
        run: timeout 120s agda --safe Exotic/ERL/FullCoupled/FiniteUniversalBoundary.agda

      - name: Check signed divisibility bridge
        run: timeout 120s agda --safe Exotic/ERL/FullCoupled/SignedDivisibilityBridge.agda

      - name: Check sparse accumulation complexity
        run: timeout 120s agda --safe Exotic/ERL/FullCoupled/FiniteSparseAccumulationComplexity.agda

