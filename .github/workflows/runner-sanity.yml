# Plain runner smoke test; independent of Agda and proof tooling.
name: Runner sanity
on:
  push:
    branches: ['main']
permissions:
  contents: read
jobs:
  sanity:
    runs-on: ubuntu-latest
    steps:
      - run: echo runner-sanity-pass
